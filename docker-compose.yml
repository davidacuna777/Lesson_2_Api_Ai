services:
  app:
    build: .
    container_name: leccion2-factory-deepseek-telegram
    env_file: .env
    environment:
      - HTTP_PORT=${HTTP_PORT:-8080}
    ports:
      - "${HTTP_PORT:-8080}:${HTTP_PORT:-8080}"
    restart: unless-stopped
